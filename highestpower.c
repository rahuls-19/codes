#include<stdio.h>
#include<math.h>
int main(){
	int n;
	scanf("%d",&n);
	int flag = 0;
	while(n>0){
		while( n > 0 && n % 2 == 0)
		count++;
		while(n > 0  && n % 2 == 1){
			count++;
			temp = count;
			
		}
	}
	 if (n < 1)
        printf("0\n");
    else
    	printf("%d\n",temp);
	
	
	
	return 0;
}
